# Buggy Cars Rating – Playwright JS

## Prerequisites
- Node.js 18+ installed

## Setup

npm install
npx playwright install


## Run tests

npm test

Run headed:

npm run test:headed


Show HTML report:

npm run report



Tests run on Chromium, Firefox, and WebKit using `playwright.config.js`.


How to run on any system
Put all files in a folder.

In that folder, run:

npm install

npx playwright install

npm test